@model web_programlama.Models.Randevu

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Randevu</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="RandevuZamani" class="control-label"></label>
                <input asp-for="RandevuZamani" class="form-control" />
                <span asp-validation-for="RandevuZamani" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label for="IslemId">Hizmet Seçiniz</label>
                <select asp-for="IslemId" class="form-control" asp-items="ViewBag.Islemler"></select>
            </div>

            <div class="form-group">
                <label for="CalisanId">Çalışan Seçiniz</label>
                <select asp-for="CalisanId" class="form-control" asp-items="ViewBag.Calisanlar"></select>
            </div>

            <div class="form-group">
                <label for="KullaniciId">Müşteri Seçiniz</label>
                <select asp-for="KullaniciId" class="form-control" asp-items="ViewBag.KullaniciIdler"></select>
            </div>

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
